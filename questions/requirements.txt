numpy>=1.24
scikit-learn>=1.3

# Optional: semantic similarity in the recommender. If omitted, the
# embedder soft-disables and the semantic_sim term contributes zero.
# The rest of the recommender (plan alignment, feature match, freshness,
# insurer match, urgency) continues to work.
sentence-transformers>=2.2
